# Models

This starter project currently uses JSON files in `backend/data/` instead of a database.

This folder is included so future collaborators can easily add:

- `User.js`
- `Investment.js`
- `Contact.js`

If you later move to MongoDB or another database, this is the best place to put those models.
